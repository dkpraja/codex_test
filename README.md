# ToyForge Studio Landing Page

Static landing page showcasing ready-made toy collections and custom toy services.

## Running tests
1. Install dependencies: `pip install -r requirements.txt`
2. Run the test suite: `python -m pytest`
